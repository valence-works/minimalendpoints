<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Referencing MinimalEndpoints.OpenApi brings its XML-comment source generator, which emits
         interceptors and needs its namespace opted in. -->
    <InterceptorsNamespaces>$(InterceptorsNamespaces);Microsoft.AspNetCore.OpenApi.Generated</InterceptorsNamespaces>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../../src/MinimalEndpoints/MinimalEndpoints.csproj" />
    <ProjectReference Include="../../src/MinimalEndpoints.OpenApi/MinimalEndpoints.OpenApi.csproj" />
    <ProjectReference Include="../../src/MinimalEndpoints.Testing/MinimalEndpoints.Testing.csproj" />

    <!--
      The generator runs over this project's own endpoint fixtures, so its output is under test.
      Its assembly is also referenced directly (ReferenceOutputAssembly defaults to true) so tests
      can drive it with a GeneratorDriver and assert its diagnostics: an unmappable fixture class
      cannot live in this assembly, because the reflective-scan tests map the whole assembly and
      would then throw at startup.
    -->
    <ProjectReference Include="../../src/MinimalEndpoints.Generator/MinimalEndpoints.Generator.csproj"
                      OutputItemType="Analyzer" />
  </ItemGroup>

</Project>
